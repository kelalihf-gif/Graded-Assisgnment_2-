
# Question 1 — analyze.sh

Script `analyze.sh` analyzes a single file or directory and prints:
- For files: number of lines, words, characters
- For directories: total files (non-recursive) and number of `.txt` files

Includes placeholders: `commands.txt`, `output.txt`, `explanation.md`, and `screenshots/`.
